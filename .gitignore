bin/
tmp/
dist/
web/dist/
editor-ui/dist/
# Keep a placeholder in the embed dir so `//go:embed all:dist` always compiles;
# real build output stays ignored. (Re-include the dir that `dist/` excluded.)
!internal/webui/dist/
internal/webui/dist/*
!internal/webui/dist/.gitkeep
# TinyGo WASM build artifacts (produced by `make wasm`)
web/public/mindbase.wasm
web/public/wasm_exec.js
node_modules/
web/node_modules/
mobile/node_modules/
mobile/ios/
mobile/android/
macos/build/
macos/lib/
mobile/native/**/*.a
mobile/native/**/*.so
*.app
.DS_Store
package-lock.json
web/package-lock.json
mobile/package-lock.json
.cursor/
.serena/
.zed/
# Top-level vault data dir only — anchored so it doesn't also match internal/vault/.
/vault/
/.expo
.worktrees/
.superpowers/
